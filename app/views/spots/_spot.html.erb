<div id="<%= dom_id spot %>">
  <div class="contents">
    <div>
      <%= link_to spot.name, spot_path(spot) %>
    </div>
    <div>
      <%= image_tag(url_for(spot.photo), width: '100%') if spot.photo.present? %>
    </div>
  </div>
</div>
